<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cipri.fitnesstracker">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogIn" />
        <activity android:name=".ContNou" />
        <activity android:name=".Profil" />
        <activity android:name=".AdaugaActivitate" />
        <activity android:name=".Istoric" />
        <activity android:name=".Despre" />
        <activity android:name=".Meniu" />
        <activity android:name=".Masa" />
        <activity android:name=".Alimente"></activity>
    </application>

</manifest>